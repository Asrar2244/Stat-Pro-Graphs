/**
 * Styles for ProjectAndType component
 */

import { makeStyles } from '@fluentui/react-components';

export const useProjectAndTypeStyles = makeStyles({
  container: {
    display: 'flex',
    flexDirection: 'column',
    gap: '16px',
  },
});

