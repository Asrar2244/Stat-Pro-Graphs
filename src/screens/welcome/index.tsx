import { FC } from 'react';

export const WelcomePage: FC = () => {
  //Todo: need to replace with specified path
  return <webview src="https://pages.github.com" style={{ width: '100%', height: '100%' }} />;
};
